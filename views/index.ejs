<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The World of Magic</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap"
        rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap"
        rel="stylesheet">
</head>

<body>

    <h1>Explore the World of Magic</h1>
    <div id="flex-container">
        <div id="potion">
            <h2>Choose your potion</h2>
            <img src="images/potion.jpg" alt="Magic cauldron">
            <% if (locals.content) { %>
                <p><i>name:</i>
                    <%= content.attributes.name %>
                </p>
                <p><i>characteristics:</i>
                    <%= content.attributes.characteristics || "None" %>
                </p>
                <p><i>effect:</i>
                    <%= content.attributes.effect || "None" %>
                </p>
                <p class="last"><i>ingredients:</i>
                    <%= content.attributes.ingredients || "None" %>
                </p>
                <% } %>
                    <% if (locals.errorP) { %>
                        <p>
                            <%= errorP %>
                        </p>
                        <% } %>
                            <form action="/potion" method="post">
                                <input type="submit" value="Go">
                            </form>
        </div>

        <div id="spell">
            <h2>Choose your spell</h2>
            <img src="images/spell.jpg" alt="Spell book">
            <% if (locals.info) { %>
                <p><i>name:</i>
                    <%= info.attributes.name %>
                </p>
                <p><i>effect:</i>
                    <%= info.attributes.effect || "None" %>
                </p>
                <p> <i>incantation:</i>
                    <%= info.attributes.incantation || "None" %>
                </p>
                <p class="last"><i>hand:</i>
                    <%= info.attributes.hand || "None" %>
                </p>
                <% } %>
                    <% if (locals.errorS) { %>
                        <p>
                            <%= errorS %>
                        </p>
                        <% } %>
                            <form action="/spell" method="post">
                                <input type="submit" value="Go">
                            </form>
        </div>
    </div>
</body>

</html>